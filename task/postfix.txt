Есть такая страница, и rebuildGUI логика работала стабильно чтобы 
- в портретном рисовать 3 строки
- в альбомном рисовать 8 строк

Вся структура расположения блоков описана в методах блока GUI Preload 
- removeOverflowBtnsMobile();
- checkLastRow();

Но при изменении ориентации, мне нужно было сохранить кнопку из endGroup (которая туда попадала),
и попробовал сделать так, чтобы она оставалась последней при новом изменении.
- Я не делаю shufflе элементов с этой стратегией, а рисую из временного массива
- Я добавил fixLogic и метод reOrientFix в rebuildGUI, чтобы находить эту кнопку

Вроде структурно checkLastRow нормально отрабатывает после reOrientFix и рисует корректное число строк,
но я заметил, что визуально это работает не так чисто, как при инициализации (что в портретном, что в альбомном).


Я считаю, что когда недостаточно кнопок в checkLastRow ( force ), после смены ориентации он не берёт кнопок из внутреннего массива, чтобы точнее собрать ряды кнопок (Fallback этого не предусматривает, так как работает по исходному массиву). В то время как при первой случайной генерации всё проходит чисто, так как там забитие кнопками происходит до тех пор пока не придёт к TARGET кол-ву строк (checkLastRow это уже пост-метод чистки).

Несмотря на то, что всё работает, я ищу как можно сделать  checkLastRow в режиме force более органичным.
Возможно доработать форсировать или ещё один тип fallback который будет работать при возможности добавления элементов, прежде чем объединить в одну строку.