Суть html документа: интерфейс из кнопок разной ширины во Flexbox-контейнере (#buttonContainer).

Основные заметки:
1. Геометрия: Родитель .fswrap центрирует всё через align-items: center.
2. Строгое поведение на мобилках через MOBILE_TARGET_ROW = 8.
3. Подсчёт строк через getRowData() используя new Set(offsetTop).
4. Принципы удаления: removeOverflowBtnsMobile использует Backtracking (откат). Если удаление кнопки схлопывает 8 строк в 7 — возвращаем кнопку на место и выходим. 
5. Удаляем только кнопки с data-st="lock" - те что заблокированы от пользователя
6. Работа с конечной строкой через checkLastRow - разное поведение applyFix для мобилки и десктопа
7. Ghosts: Вместо перемещения кнопки-соседа, мы клонируем её, делаем клон прозрачным (opacity: 0), а оригинал сажаем в endGroup. Это держит высоту и не дает верстке прыгать.
- ГОСТ НУЖЕН ЧТОБЫ ДЕРЖАТЬ ПОРТРЕТ ЧИСТЫМ ПРИ ИНИЦИАЛИЗАЦИИ
8. Если applyFix (подселение или объединение ряда) меняет количество строк (стало 7 или 9) — делаем Rollback .

Основной принцип выравнивания описан в блоке GUI Preload
Вся инициализация в GUI Updater 

Стоит обратить внимание на ghost класс кнопок, которые создаются для сохранения верстки. 
В теории для них прописана логика защиты в restoreSourceBtns 

Главное, чтобы ты точно понимал:
- стараться не менять имена уже данным переменным. предложить аккуратные решения по интеграции
- структурно весь блок GUI Preload уже написан и его лучше не трогать.
- основная проблема в процессе инициализации и работы с SOURCE_BUTTONS и методом внутреннего хранения кнопок.
- adjustFontByCount и removeDeskBtnOnMobile - это pre стадии. Их лучше держать перед сохранения в глобальный массив. 
То есть мы сразу задаём размер кнопкам и удаляем то, что мы не должны видеть на текущей платформе
- проверить ghost кнопки и по возможности предложить метод аккуратного удаления (возможно общий вспомогательный метод который
будет при глобальной restore вызываться). Его логика должна повторять решения которые уже были написаны - как пример возврат на строчках





